# 🔧 Быстрая настройка прокси для OpenAI

## ✅ Что уже сделано

1. ✅ Добавлена поддержка прокси в `OpenAIClient`
2. ✅ Добавлена настройка `OPENAI_PROXY` в конфигурацию
3. ✅ Добавлена библиотека `httpx` в `requirements.txt`
4. ✅ Создан скрипт для тестирования прокси

## 🚀 Быстрый старт

### Шаг 1: Установите зависимости (если еще не установлены)

```bash
pip install httpx
# Или обновите все зависимости
pip install -r requirements.txt
```

### Шаг 2: Найдите прокси сервер

Варианты:
- **Платные прокси**: Bright Data, Smartproxy, Oxylabs
- **VPN с прокси**: Многие VPN сервисы предоставляют HTTP прокси
- **Бесплатные**: free-proxy-list.net (не рекомендуется для продакшена)

### Шаг 3: Протестируйте прокси

```bash
./scripts/test_proxy.sh http://proxy-host:port
```

### Шаг 4: Настройте на сервере

**Вариант A: Через переменную окружения (рекомендуется)**

```bash
# На сервере
export OPENAI_PROXY="http://proxy-host:port"
# Или с авторизацией
export OPENAI_PROXY="http://username:password@proxy-host:port"

# Для постоянной настройки добавьте в systemd service
sudo nano /etc/systemd/system/ai-manager.service
# Добавьте в секцию [Service]:
Environment="OPENAI_PROXY=http://proxy-host:port"

sudo systemctl daemon-reload
sudo systemctl restart ai-manager
```

**Вариант B: Через файл конфигурации**

Отредактируйте `app/config.py`:
```python
OPENAI_PROXY = "http://proxy-host:port"
```

### Шаг 5: Проверьте работу

```bash
# Проверьте логи
sudo journalctl -u ai-manager -f

# Или через веб-интерфейс попробуйте загрузить файл
```

## 📋 Формат прокси

- **HTTP**: `http://proxy-host:port`
- **С авторизацией**: `http://username:password@proxy-host:port`
- **HTTPS**: `https://proxy-host:port` (если поддерживается)

## 🔍 Проверка

После настройки прокси, все запросы к OpenAI будут идти через указанный прокси.

Если прокси не указан, приложение работает как обычно (без прокси).

## 📚 Подробная документация

См. `scripts/setup_proxy.md` для детальной информации.

