# 🎯 Рекомендации по выбору прокси для OpenAI

## ✅ Лучшие варианты из вашего списка

### 1. **США - Санта-Клара** (Рекомендуется) ⭐
```
http://205.164.46.6:3128
```
- **Протоколы**: HTTPS, SOCKS5
- **Скорость**: Высокая (254 мсек)
- **Анонимность**: Высокая
- **Почему**: OpenAI лучше всего работает из США
- **Использование**: 5824 из 7734

### 2. **HTTPS прокси** (Альтернатива)
```
http://49.48.94.235:8080
```
- **Протоколы**: HTTPS
- **Скорость**: Средняя (322 мсек)
- **Локация**: Таиланд, Накхонратчасима
- **Использование**: 1536 из 5552

```
http://210.79.146.234:8080
```
- **Протоколы**: HTTPS
- **Скорость**: Высокая (254 мсек)
- **Использование**: 197 из 704

## 🚀 Быстрая настройка

### Вариант 1: Автоматическая настройка (рекомендуется)

```bash
# Для прокси из США
./scripts/configure_proxy_from_list.sh http://205.164.46.6:3128

# Или для HTTPS прокси
./scripts/configure_proxy_from_list.sh http://49.48.94.235:8080
```

### Вариант 2: Ручная настройка

1. **Протестируйте прокси:**
```bash
./scripts/test_proxy.sh http://205.164.46.6:3128
```

2. **Настройте на сервере:**
```bash
ssh root@95.81.96.59

# Добавьте в systemd service
sudo nano /etc/systemd/system/ai-manager.service

# В секции [Service] добавьте:
Environment="OPENAI_PROXY=http://205.164.46.6:3128"

# Перезапустите
sudo systemctl daemon-reload
sudo systemctl restart ai-manager
```

## ⚠️ Важные замечания

### Протоколы
- **HTTP/HTTPS**: Работают сразу, используйте формат `http://host:port`
- **SOCKS5**: Требует установки `httpx[socks]`:
  ```bash
  pip install 'httpx[socks]'
  ```
  Формат: `socks5://host:port`

### Проверка работы
После настройки проверьте:
1. Логи сервиса: `sudo journalctl -u ai-manager -f`
2. Внешний IP через прокси: `curl -x http://proxy:port ifconfig.me`
3. Тестовый запрос к OpenAI через веб-интерфейс

### Если прокси не работает
1. Проверьте доступность: `curl -x http://proxy:port https://api.openai.com`
2. Попробуйте другой прокси из списка
3. Проверьте логи на ошибки подключения

## 📊 Сравнение прокси из списка

| Прокси | Локация | Скорость | Протоколы | Рекомендация |
|--------|---------|----------|-----------|--------------|
| 205.164.46.6:3128 | США, Санта-Клара | Высокая (254ms) | HTTPS, SOCKS5 | ⭐ Лучший для OpenAI |
| 49.48.94.235:8080 | Таиланд | Средняя (322ms) | HTTPS | ✅ Хороший вариант |
| 210.79.146.234:8080 | - | Высокая (254ms) | HTTPS | ✅ Хороший вариант |
| 103.54.217.82:8199 | Индонезия | Высокая (335ms) | SOCKS5 | ⚠️ Требует httpx[socks] |

## 🔄 Смена прокси

Если нужно сменить прокси:

```bash
# Отредактируйте service файл
sudo nano /etc/systemd/system/ai-manager.service

# Измените значение OPENAI_PROXY
Environment="OPENAI_PROXY=http://новый-прокси:порт"

# Перезапустите
sudo systemctl daemon-reload
sudo systemctl restart ai-manager
```

## ❌ Отключение прокси

```bash
# Удалите строку Environment="OPENAI_PROXY=..." из service файла
sudo nano /etc/systemd/system/ai-manager.service

# Перезапустите
sudo systemctl daemon-reload
sudo systemctl restart ai-manager
```

