# ИИ Менеджер для выбора станков

Веб-приложение для автоматического заполнения технического задания (ТЗ) с помощью ИИ на основе документа заказчика.

## 📋 Описание

Система автоматизирует процесс заполнения технического задания:
1. **Загрузка документа** - пользователь загружает ТЗ заказчика (PDF, Word, Excel)
2. **Конвертация** - документ автоматически конвертируется в текст
3. **Построение промпта** - система подставляет шаблон JSON и текст ТЗ в промпт
4. **Обработка ИИ** - промпт отправляется в OpenAI API (GPT-4o)
5. **Результат** - пользователь получает заполненный JSON файл

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение (если еще не создано)
python3 -m venv venv

# Активируйте окружение
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка OpenAI API

Установите переменную окружения с API ключом:

```bash
export OPENAI_API_KEY='your-api-key-here'
```

Или создайте файл `.env` (см. `.env.example`).

### 3. Убедитесь, что файлы конфигурации на месте

- `Промпт.txt` - шаблон промпта для ИИ (должен содержать плейсхолдеры)
- `TZ.json` - шаблон JSON с параметрами для заполнения (должен быть валидным JSON)

### 4. Запуск веб-интерфейса

```bash
# Через скрипт (если есть)
./start_web.sh

# Или вручную
source venv/bin/activate
python web_app.py
```

Откройте в браузере: **http://127.0.0.1:5000**

## 📋 Процесс работы

1. Пользователь загружает документ с ТЗ заказчика через веб-интерфейс
2. Система конвертирует документ в текст (поддерживаются PDF, DOC, DOCX, XLS, XLSX, TXT)
3. Система строит промпт:
   - Берет шаблон из `Промпт.txt`
   - Подставляет `TZ.json` вместо плейсхолдера "Шаблон JSON:"
   - Подставляет текст ТЗ вместо плейсхолдера "Текст ТЗ:"
4. Промпт отправляется в OpenAI API
5. ИИ заполняет JSON и возвращает результат
6. Пользователь скачивает заполненный JSON файл

## 📁 Структура проекта

```
AI Manager/
├── src/
│   ├── document_converter.py  # Конвертер документов
│   ├── prompt_builder.py      # Построение промпта
│   └── ai_client.py           # Клиент OpenAI API
├── templates/
│   └── index.html            # Веб-интерфейс
├── static/
│   ├── css/style.css         # Стили
│   └── js/main.js            # JavaScript
├── uploads/                  # Загруженные файлы (создается автоматически)
├── converted/                # Сконвертированные файлы (создается автоматически)
├── results/                  # Результаты обработки (создается автоматически)
├── Промпт.txt                # Шаблон промпта для ИИ
├── TZ.json                   # Шаблон JSON для заполнения
├── web_app.py                # Flask приложение
├── requirements.txt          # Зависимости
└── README.md                 # Документация
```

## 🔧 Настройка

### Модель OpenAI

По умолчанию используется `gpt-4o`. Чтобы изменить, установите переменную окружения:

```bash
export OPENAI_MODEL=gpt-4-turbo
```

Или измените в коде `src/ai_client.py`:

```python
ai_client = OpenAIClient(model="gpt-4-turbo")
```

### Максимальный размер файла

По умолчанию: 50 МБ. Измените в `web_app.py`:

```python
app.config['MAX_CONTENT_LENGTH'] = 100 * 1024 * 1024  # 100 МБ
```

### Порт и хост

Измените в `web_app.py` (в конце файла):

```python
app.run(debug=True, host='127.0.0.1', port=5000)
```

- `host='0.0.0.0'` - доступ из сети
- `host='127.0.0.1'` - только локально
- `port=5000` - номер порта

## 📄 Конвертер документов

### Поддерживаемые форматы

- **PDF** (.pdf) - через PyPDF2
- **Word** (.docx) - через python-docx
- **Word (старый)** (.doc) - требует antiword или LibreOffice
- **Excel** (.xlsx) - через pandas/openpyxl
- **Excel (старый)** (.xls) - через pandas/xlrd
- **Text** (.txt) - простое копирование

### Использование конвертера отдельно

```bash
# Базовое использование
python convert_document.py document.pdf

# С указанием выходного файла
python convert_document.py report.docx -o output.txt
```

### Программный интерфейс

```python
from src.document_converter import DocumentConverter

converter = DocumentConverter()
output_file = converter.convert("document.pdf", "output.txt")
```

### Поддержка DOC файлов

Для конвертации старых DOC файлов (Word 97-2003) требуется один из инструментов:

1. **antiword** (рекомендуется, быстрый):
   ```bash
   sudo apt-get install antiword
   ```

2. **LibreOffice** (универсальный):
   ```bash
   sudo apt-get install libreoffice
   ```

Конвертер автоматически попробует использовать доступные инструменты.

## 🎨 Веб-интерфейс

### Возможности

- ✅ Загрузка файлов через веб-интерфейс (drag & drop или выбор через кнопку)
- ✅ Поддержка PDF, DOC, DOCX, XLS, XLSX, TXT
- ✅ Автоматическая конвертация
- ✅ Индикация процесса (прогресс-бар)
- ✅ Обработка ИИ с отображением прогресса
- ✅ Скачивание результата
- ✅ Отображение статистики использования токенов
- ✅ Обработка ошибок с понятными сообщениями

### Пример использования

1. Откройте http://127.0.0.1:5000
2. Нажмите "Выбрать файл" или перетащите файл
3. Выберите документ (PDF, Word, Excel)
4. Нажмите "Обработать с помощью ИИ"
5. Дождитесь завершения (конвертация → построение промпта → обработка ИИ)
6. Скачайте заполненный JSON файл

## 📊 Использование токенов

Интерфейс показывает информацию об использовании токенов:
- **Промпт токены** - размер отправленного промпта
- **Completion токены** - размер ответа от ИИ
- **Всего токенов** - общее использование

Эта информация помогает отслеживать расходы на API.

## ⚠️ Важно

- Убедитесь, что `Промпт.txt` и `TZ.json` находятся в корне проекта
- API ключ OpenAI должен быть установлен через переменную окружения
- Файл `TZ.json` должен быть валидным JSON
- Файл `Промпт.txt` должен содержать плейсхолдеры:
  - `Шаблон JSON:\n\n` - будет заменен на содержимое TZ.json
  - `Текст ТЗ:\n\n` - будет заменен на текст из документа
- Для DOC файлов требуется `antiword` или `LibreOffice`

## 🔒 Безопасность (для продакшена)

⚠️ **Важно для продакшена:**

1. Измените `app.secret_key` в `web_app.py` на случайную строку
2. Используйте WSGI сервер (gunicorn, uWSGI) вместо встроенного Flask сервера
3. Настройте HTTPS
4. Ограничьте доступ по IP или используйте аутентификацию
5. Не храните API ключи в коде - используйте переменные окружения

## 🐛 Устранение неполадок

### Ошибка "API ключ не найден"

```bash
# Проверьте, установлен ли ключ
echo $OPENAI_API_KEY

# Установите ключ
export OPENAI_API_KEY='your-key'
```

### Ошибка конвертации DOC

```bash
# Установите antiword
sudo apt-get install antiword

# Или LibreOffice
sudo apt-get install libreoffice
```

### Порт занят

Измените порт в `web_app.py`:

```python
app.run(debug=True, host='127.0.0.1', port=5001)
```

### Файлы не загружаются

- Проверьте права доступа к папкам `uploads/`, `converted/`, `results/`
- Убедитесь, что формат файла поддерживается
- Проверьте максимальный размер файла в настройках

### TZ.json пустой или невалидный

- Убедитесь, что файл `TZ.json` содержит валидный JSON
- Проверьте, что файл не пустой
- Используйте JSON валидатор для проверки синтаксиса

### Зависимости не установлены

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Проверка установки OpenAI

```bash
source venv/bin/activate
pip list | grep openai
```

### Логи сервера

Смотрите вывод в консоли при запуске `web_app.py` для диагностики ошибок.

## 📝 Ограничения

- **Сложные PDF** с изображениями - извлекается только текст
- **Защищенные файлы** - могут не конвертироваться
- **DOC файлы** - требуют antiword или LibreOffice
- **Большие файлы** - могут занимать много времени на обработку

## 🔍 Отладка

Если что-то не работает, проверьте:

1. **API ключ установлен?**
   ```bash
   echo $OPENAI_API_KEY
   ```

2. **Файлы на месте?**
   ```bash
   ls -la Промпт.txt TZ.json
   ```

3. **Зависимости установлены?**
   ```bash
   source venv/bin/activate
   pip list | grep -E "openai|flask|pandas|PyPDF2"
   ```

4. **Логи сервера** - смотрите вывод в консоли при запуске `web_app.py`

5. **Валидность JSON** - проверьте `TZ.json` с помощью JSON валидатора

## 📚 Дополнительная информация

- Все загруженные файлы сохраняются в папке `uploads/`
- Сконвертированные файлы сохраняются в папке `converted/`
- Результаты обработки сохраняются в папке `results/`
- Все папки создаются автоматически при первом запуске

