# Скрипты для работы с проектом

## 📋 Описание скриптов

### 1. `push_to_github.sh`
Загружает изменения на GitHub.

**Использование:**
```bash
./scripts/push_to_github.sh
```

**Что делает:**
- Проверяет наличие изменений
- Добавляет все файлы в git
- Создает коммит (с запросом сообщения)
- Отправляет на GitHub

### 2. `deploy.sh`
Обновляет приложение на сервере через GitHub.

**Использование:**
```bash
./scripts/deploy.sh
```

**Что делает:**
- Получает последние изменения из GitHub
- Обновляет Python зависимости
- Проверяет структуру директорий
- Проверяет наличие API ключа
- Перезапускает сервис
- Проверяет работоспособность

### 3. `update_server.sh`
Полный цикл: коммит → GitHub → деплой на сервер.

**Использование:**
```bash
./scripts/update_server.sh
```

**Что делает:**
- Загружает изменения на GitHub
- Обновляет приложение на сервере

## ⚙️ Настройка

### Перед первым использованием:

1. **Настройте пароль SSH в `deploy.sh`:**
   ```bash
   SSH_PASS="your-password-here"
   ```

2. **Или лучше используйте SSH ключи:**
   - Создайте SSH ключ: `ssh-keygen -t ed25519`
   - Скопируйте на сервер: `ssh-copy-id root@95.81.96.59`
   - Удалите строку `SSH_PASS` из скрипта
   - Измените `remote_exec()` на обычный `ssh`

3. **Проверьте URL репозитория:**
   ```bash
   git remote -v
   ```

## 🔒 Безопасность

⚠️ **Важно:** Скрипт `deploy.sh` содержит пароль в открытом виде. 

**Рекомендации:**
- Используйте SSH ключи вместо паролей
- Или храните пароль в переменной окружения
- Или используйте `.env` файл (не коммитьте его!)

## 📝 Примеры использования

### Разработка локально:
```bash
# Внесли изменения в код
# Загружаем на GitHub
./scripts/push_to_github.sh
```

### Деплой на сервер:
```bash
# После загрузки на GitHub
./scripts/deploy.sh
```

### Полный цикл:
```bash
# Коммит + GitHub + Деплой одной командой
./scripts/update_server.sh
```

