# 🔧 Настройка прокси для OpenAI

## 📋 Варианты настройки

### Вариант 1: Через переменную окружения (Рекомендуется)

Установите переменную окружения на сервере:

```bash
# Для текущей сессии
export OPENAI_PROXY="http://proxy-host:port"

# Или с авторизацией
export OPENAI_PROXY="http://username:password@proxy-host:port"

# Для постоянной настройки добавьте в ~/.bashrc или /etc/environment
echo 'export OPENAI_PROXY="http://proxy-host:port"' >> ~/.bashrc
```

### Вариант 2: Через файл конфигурации

Добавьте в `app/config.py`:

```python
OPENAI_PROXY = "http://proxy-host:port"
```

### Вариант 3: Через systemd service (для продакшена)

Отредактируйте файл сервиса `/etc/systemd/system/ai-manager.service`:

```ini
[Service]
Environment="OPENAI_PROXY=http://proxy-host:port"
```

Затем перезапустите:
```bash
sudo systemctl daemon-reload
sudo systemctl restart ai-manager
```

---

## 🌐 Где взять прокси?

### Бесплатные прокси (не рекомендуется для продакшена)
- https://www.proxy-list.download/
- https://free-proxy-list.net/
- ⚠️ Низкая надежность, могут быть медленными

### Платные прокси (рекомендуется)
- **Bright Data** (бывший Luminati): https://brightdata.com/
- **Smartproxy**: https://smartproxy.com/
- **Oxylabs**: https://oxylabs.io/
- **Proxy-Cheap**: https://proxy-cheap.com/

### Прокси из VPN сервисов
- Многие VPN сервисы предоставляют HTTP/HTTPS прокси
- Проверьте документацию вашего VPN провайдера

---

## 🔍 Проверка работы прокси

### Тест 1: Проверка доступности прокси

```bash
curl -x http://proxy-host:port https://api.openai.com
```

### Тест 2: Проверка через Python

```python
import httpx

proxy = "http://proxy-host:port"
response = httpx.get(
    "https://api.openai.com",
    proxies={"http://": proxy, "https://": proxy},
    timeout=10
)
print(response.status_code)
```

### Тест 3: Проверка в приложении

После настройки прокси, проверьте логи приложения:
```bash
tail -f /var/log/ai-manager/app.log
```

Или через веб-интерфейс попробуйте загрузить файл и проверить, работает ли OpenAI.

---

## ⚙️ Настройка на сервере

### Шаг 1: Выберите прокси сервер

Выберите надежный прокси из списка выше или используйте свой.

### Шаг 2: Установите переменную окружения

```bash
# Подключитесь к серверу
ssh root@95.81.96.59

# Установите прокси (замените на ваш)
export OPENAI_PROXY="http://your-proxy-host:port"

# Проверьте
echo $OPENAI_PROXY
```

### Шаг 3: Добавьте в systemd service

```bash
# Отредактируйте сервис
sudo nano /etc/systemd/system/ai-manager.service

# Добавьте в секцию [Service]:
Environment="OPENAI_PROXY=http://your-proxy-host:port"

# Перезагрузите и перезапустите
sudo systemctl daemon-reload
sudo systemctl restart ai-manager

# Проверьте статус
sudo systemctl status ai-manager
```

### Шаг 4: Проверьте логи

```bash
sudo journalctl -u ai-manager -f
```

---

## 🐛 Решение проблем

### Проблема: "Connection refused"
- Проверьте, что прокси сервер доступен
- Проверьте порт и адрес
- Убедитесь, что прокси не требует авторизации

### Проблема: "Timeout"
- Прокси может быть перегружен
- Попробуйте другой прокси
- Увеличьте timeout в коде

### Проблема: "Proxy authentication required"
- Используйте формат: `http://username:password@proxy-host:port`
- Проверьте правильность учетных данных

### Проблема: "SSL/TLS error"
- Некоторые прокси требуют специальной настройки SSL
- Попробуйте другой прокси
- Проверьте сертификаты

---

## 📝 Примеры конфигурации

### Пример 1: Простой HTTP прокси
```bash
export OPENAI_PROXY="http://proxy.example.com:8080"
```

### Пример 2: Прокси с авторизацией
```bash
export OPENAI_PROXY="http://user:pass@proxy.example.com:8080"
```

### Пример 3: SOCKS5 прокси (требует дополнительной настройки)
```bash
# Для SOCKS5 нужна библиотека requests[socks] или httpx[socks]
pip install httpx[socks]
export OPENAI_PROXY="socks5://proxy.example.com:1080"
```

---

## ✅ Чек-лист настройки

- [ ] Выбран прокси сервер
- [ ] Прокси протестирован (curl работает)
- [ ] Переменная окружения установлена
- [ ] Systemd service обновлен
- [ ] Сервис перезапущен
- [ ] Логи проверены
- [ ] Тестовый запрос к OpenAI выполнен успешно

---

## 🔄 Отключение прокси

Если нужно отключить прокси:

```bash
# Удалите переменную окружения
unset OPENAI_PROXY

# Или установите пустое значение
export OPENAI_PROXY=""

# Перезапустите сервис
sudo systemctl restart ai-manager
```

Или удалите строку из systemd service файла.

