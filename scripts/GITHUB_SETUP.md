# Настройка GitHub для загрузки изменений

## 🔐 Вариант 1: Personal Access Token (рекомендуется)

1. **Создайте токен на GitHub:**
   - Перейдите: https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Выберите scope: `repo` (полный доступ)
   - Скопируйте токен (начинается с `ghp_...`)

2. **Используйте токен при push:**
   ```bash
   git push -u origin main
   # Username: ваш-username
   # Password: вставьте токен (не пароль!)
   ```

## 🔑 Вариант 2: SSH ключи

1. **Создайте SSH ключ (если нет):**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. **Добавьте ключ в GitHub:**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   # Скопируйте вывод и добавьте в:
   # https://github.com/settings/keys
   ```

3. **Измените remote на SSH:**
   ```bash
   git remote set-url origin git@github.com:HappyOu-cmd/AI_MANAGER.git
   ```

4. **Теперь push будет работать без пароля:**
   ```bash
   git push -u origin main
   ```

## 📝 Использование скрипта

После настройки аутентификации можно использовать:

```bash
./scripts/push_to_github.sh
```

Скрипт автоматически:
- Добавит все изменения
- Создаст коммит
- Отправит на GitHub

