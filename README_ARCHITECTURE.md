# Новая архитектура AI Manager

## ✅ Что сделано

1. **Создана модульная структура папок**
   - `app/routes/` - маршруты разделены по функциональности
   - `app/services/` - бизнес-логика
   - `app/core/` - переиспользуемые компоненты
   - `app/utils/` - утилиты и валидаторы

2. **Система конфигурации**
   - Разные конфигурации для dev/prod/test
   - Все пути в одном месте
   - Поддержка переменных окружения

3. **Базовые классы для расширяемости**
   - `BaseConverter` - для новых форматов документов
   - `BaseAIClient` - для новых AI провайдеров

4. **Обработка ошибок**
   - Кастомные исключения
   - Централизованные обработчики
   - Логирование

5. **Валидация**
   - Валидатор файлов
   - Проверка расширений и размера

## 📁 Структура

```
app/
├── __init__.py              # Factory функция
├── config.py                # Конфигурация
├── routes/                  # Маршруты (Blueprints)
│   ├── main.py             # Главная страница
│   ├── upload.py            # Загрузка файлов
│   └── download.py         # Скачивание
├── services/                # Бизнес-логика
│   ├── document_service.py  # Обработка документов
│   └── ai_service.py        # Работа с AI
├── core/                    # Ядро
│   ├── converters/          # Конвертеры
│   ├── ai/                  # AI клиенты
│   ├── builders/            # Построители
│   └── exporters/           # Экспортеры
└── utils/                   # Утилиты
    ├── validators.py
    ├── exceptions.py
    └── error_handlers.py
```

## 🚀 Запуск

### Новый способ (рекомендуется):
```bash
python run.py
```

### Старый способ (для обратной совместимости):
```bash
python web_app.py  # Пока работает, но будет удален
```

## 🔄 Миграция

Старый код в `src/` и `web_app.py` можно оставить для обратной совместимости.

Новая структура работает параллельно.

## 📝 Следующие шаги

1. Протестировать новую структуру
2. Обновить импорты в существующих файлах (если нужно)
3. Удалить старый код после полной миграции
4. Добавить тесты в `tests/`

## 💡 Преимущества новой архитектуры

- **Масштабируемость**: Легко добавлять новые компоненты
- **Читаемость**: Код разделен по функциональности
- **Тестируемость**: Каждый компонент можно тестировать отдельно
- **Поддерживаемость**: Легче находить и исправлять ошибки

